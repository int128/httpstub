- response:
    delay: 500
    headers:
      content-type: application/json
    body:
      id: ${body.id}
      name: ${body.name}
      description: user#${body.id}
      active: ${body.active as boolean}
      registeredDate: ${constants.today}
